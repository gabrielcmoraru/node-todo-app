import styled from 'styled-components';

const TodoCreateWrapper = styled.div`
  display: flex;
  align-items: center;
`;

const TodoCreateInput = styled.input`
  margin: 0 8px;
`;

const TodoCreateButton = styled.button``;

export { TodoCreateWrapper, TodoCreateInput, TodoCreateButton };
