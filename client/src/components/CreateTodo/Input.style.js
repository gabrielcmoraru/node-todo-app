import styled from 'styled-components';

const InputWrapper = styled.div`
  display: flex;
  align-items: center;
`;

const InputText = styled.input`
  margin: 0 8px;
`;

const InputButton = styled.button``;

export { InputWrapper, InputText, InputButton };
